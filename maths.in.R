#basics maths
15+15
5^3
6/2
30-20
5*5

#Mathematical functions in R
log(100)    # calculates natural log
log10(100 )# calculates log base 10
log2(8)   #calculates log base 2
exp(2)    #calculates exponential of a number
exp(2)*exp(1)
pi       # inbuilt function that gives value of pi

#trigonometry
sin(2)  #calculates sin
cos(2)  #calculates cos
tan(2)  #calculates tan
asin(0.5) #calculates sin inverse
acos(0.5) #calculates cos inverse
atan(1)  #calculates tan inverse

#rounding off 
floor(3.142)   #rounds a number down to the nearest integer
ceiling(3.142) #rounds a number up to the nearest integer
round(3.1423,2)#rounds a number to a specified number of dp
trunc(3.1423,2)#truncates decimal numbers upto a specified point
trunc(3.1423)#truncates all decimal numbers 

abs(4)#calculates absolute value(distance b2n a number&zero on the number line) of a number
abs(-3)
sqrt(100)#finds square root 

a<-c(1:10)
print(a)
sum(a)#calculates sum of a set of numbers
prod(a)#calculates the product of a set of numbers
max(a)#finds max no, in a set of numbers
min(a)#finds meam no.
mean(a)#finds mean
mode(a)#finds mode
median(a)#finds median

#Arithematic Operators
x<-10
y<-15
x+y   #add
x-y  #minus
x*y #multiply
x/y#divide
2^4#exponent sign
x%%y #displays modulus(remainder from the division)
y%%x
x%/%y #integer division(removes decimals/fractions from result of division
y%/%x

#Relational operators
x>y #greater than
x<y #less than
x>=y #greater than equal to
x<=y #less than equal to
x!=y #not equal to
x=y #equal to

#maths in complex numbers
if(FALSE){
  "complex numbers are a combination of real and imaginnery numbers
  your calculations should contain both of these after calculations
  consider:"
}
1+2i
1-2i
1*2i
1/2i
2i



